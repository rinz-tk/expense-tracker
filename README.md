# Expense Tracker

Expense Tracking App with a React frontend and a backend written in Go and Rust, which allows users to log and categorize expenses. Additionally, these expenses can be split among other users, and the expenses are automatically updated as the splits are settled.
